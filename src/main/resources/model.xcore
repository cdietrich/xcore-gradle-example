@GenModel(
    bundleManifest="false",
    modelDirectory="xcore-gradle-example/src/main/java-gen",
    complianceLevel="8.0"
)
package sample.model

import xcore.lang.GenModel

class User {
    String name

    op String sayHello(User user) {
        "Hello " + user.name
    }

}

class Viech {
	Integer a
}
